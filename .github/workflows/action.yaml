name: Deploy

on:
  push:
    branches:
      - '**'

jobs:
  deploy:
    name: Test, Build, Deploy
    runs-on: ubuntu-latest

    steps:
    - name : "Deploytag"
      uses: thaunghtike-share/deploytag@main
      id: deploytag

    - name: Checkout
      uses: actions/checkout@v2

    - name: Install Python
      uses: actions/setup-python@v4
      with:
        python-version: '3.9'
